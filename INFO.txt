POWERSHELL (Install AWS CLI)
irm https://awscli.amazonaws.com/v2/install.ps1 | iex

TERMINAL (check aws)
aws --version
aws configure
- test
- test
- us-east-1
- json

TERMINAL (connect aws to localstack)
aws --endpoint-url=http://localhost:4566 lambda list-functions

LocalStack Pro
docker pull localstack/localstack-pro
lstk start
lstk logs --follow

Patient Management/Infrastructure
bash ./localstack-deploy.sh

To Test in Postman (It will provide load balancer url once you run the LocalStack in the topbar)
e.g. lb-90cba8ac.elb.localhost.localstack.cloud